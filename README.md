# Fastify + Prisma + Redis

### Installation

1. Install recommended tsconfig.json

```
$ npm install --save-dev @tsconfig/node24
$ yarn add --dev @tsconfig/node24
```

2. Install fastify-cli

```
npm install -g fastify-cli
```

3. Install dependencies

```
$ npm i
```
